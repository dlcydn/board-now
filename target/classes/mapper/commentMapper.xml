<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.unipoint.board.dao.CommentMapper">

    <!--    the SQL Commands that want run -->
    <!-- 게시물 번호를 주면 댓글 다 지움-->
    <delete id="deleteCnoAll" parameterType="int">
        DELETE FROM comment
        WHERE bno = #{bno}
    </delete>

    <delete id="delete" parameterType="map">
        DELETE FROM comment
        WHERE cno = #{cno} AND commenter = #{commenter}
    </delete>

    <insert id="insertCno" parameterType="CommentDto">
        INSERT INTO comment (bno, pcno, comment, commenter, reg_date, up_date)
        VALUES (#{bno}, #{pcno}, #{comment}, #{commenter}, now(), now())
    </insert>

    <select id="selectCnoAll" parameterType="CommentDto">
        SELECT * FROM comment
        WHERE bno = #{bno}
        ORDER BY reg_date ASC, cno ASC
    </select>

    <select id="select" parameterType="int" resultType="CommentDto">
        SELECT * FROM comment
        WHERE cno = #{cno}
    </select>

    <select id="count" parameterType="int" resultType="int">
        SELECT count(*) FROM comment
        WHERE bno = #{bno}
    </select>

    <update id="update" parameterType="CommentDto">
        UPDATE comment
        SET comment = #{comment}, up_date = now()
        WHERE cno = #{cno} AND commenter = #{commenter}
    </update>
</mapper>